# Same-origin API (recommended behind nginx / Cloudflare tunnel)
# Leave as /api/v1 — no separate api.kedismart hostname needed.
NEXT_PUBLIC_API_URL=/api/v1

# Optional: where Next rewrites /api to during local `next start` / `next dev`
# BACKEND_URL=http://127.0.0.1:8000
